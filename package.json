{
  "name": "yhchat-public-blacklist",
  "version": "1.0.0",
  "description": "一个共享的黑名单，使各个群组免于各种威胁",
  "scripts": {
    "build": "node src/build.js",
    "prebuild": "mkdir -p public",
    "clean": "rm -rf public/*",
    "dev": "npm run build && echo '构建完成'"
  },
  "dependencies": {
    "csv-parse": "^5.5.0",
    "fs-extra": "^11.1.1",
    "iconv-lite": "^0.6.3",
    "jschardet": "^3.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}